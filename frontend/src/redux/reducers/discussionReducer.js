// TODO: should call getDiscussion function that retrieves data from backend
const INITIAL_STATE = [];

function discussionReducer(state = INITIAL_STATE, action) {
    switch(action.type) {

        default: return state;
    }
}

export default discussionReducer;